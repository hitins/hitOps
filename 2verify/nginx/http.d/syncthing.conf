server{
    # Nginx version 1.25+
    listen 443 ssl;
    http2 on;
    server_name sync.eg; # domain name

    location / {
        	proxy_pass 	          http://127.0.0.1:8384;
        	proxy_set_header      Host $host;
    	    proxy_set_header      Upgrade $http_upgrade;
    }
}
